// Auth
import { loginUser, registerUser } from "./authServices";

// Teacher
import {
  getAllTeachers,
  deleteTeacher,
  createTeacher,
  getTeacher,
} from "./teacherServices";

// Export
export {
  getTeacher,
  loginUser,
  registerUser,
  getAllTeachers,
  deleteTeacher,
  createTeacher,
};
